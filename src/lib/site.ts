export const SITE_ORIGIN = (process.env.NEXT_PUBLIC_SITE_ORIGIN ?? "https://www.laudos.ai").replace(/\/$/, "");
export const OG_ASSET_ORIGIN = (
  process.env.NEXT_PUBLIC_OG_ASSET_ORIGIN ?? "https://abridge-ditto-clone.vercel.app"
).replace(/\/$/, "");
